# Sample Project Workspace

This workspace is used by the lesson 7 coding agent demo.

Suggested tasks:

- Find `greet_user` and add friendly empty-name handling.
- Change `DEFAULT_THEME` from `light` to `dark`.
- Locate discount logic and summarize how it works.
